import React from 'react';

const withListings = (WrappedComponent:any) => {
  // const WithListings = ({ listings, ...otherProps }:any) => {
  //   // Optionally, you can add loading and error handling logic here
  //   if (!listings) {
  //     return null; // Or render a loading state or fallback UI
  //   }

  //   return listings.map((listing:any) => (
  //     <WrappedComponent key={listing.id} listing={listing} {...otherProps} />
  //   ));
  // };

  // return WithListings;
};

export default withListings;
